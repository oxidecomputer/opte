[package]
name = "opte-ioctl"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = { version = "0.2" }
libnet = { git = "https://github.com/oxidecomputer/netadm-sys" }
opte-core = { path = "../opte-core" }
postcard = { version = "0.7.0" }
serde = { version = "1.0" }
thiserror = { version = "1.0.28" }